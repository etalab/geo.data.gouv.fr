general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 6
deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "infra@beta.gouv.fr"
      - git config --global user.name "CircleCI"
      - ./deploy.sh
